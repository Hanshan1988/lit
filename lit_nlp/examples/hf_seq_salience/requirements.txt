# Requirements for hf_seq_salience
#
# Install with:
#   pip install -r requirements.txt
#
# For CUDA (replace cu121 with your CUDA version):
#   pip install torch --index-url https://download.pytorch.org/whl/cu121

# Core LIT dependencies (install from the lit_nlp package root first:
#   pip install -e /path/to/lit/  OR  pip install lit-nlp)

# HuggingFace Transformers + PyTorch
transformers>=4.40.0
accelerate>=0.30.0
torch>=2.2.0           # GPU: install via pytorch.org for your CUDA version

# Required for Qwen and other models that ship custom code
sentencepiece>=0.1.99  # tokenizer backend for many models
protobuf>=3.20.0

# Optional (improves tokenizer speed for some models)
# tokenizers>=0.19.0
